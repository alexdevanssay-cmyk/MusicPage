<?xml version="1.0" encoding="utf-8"?>
<!--
  MusicPage Android manifest.
  Permissions: RECORD_AUDIO (mic following), INTERNET + cleartext (reach the
  desktop backend over the LAN), FOREGROUND_SERVICE(_MICROPHONE) for capture
  while backgrounded.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Microphone: required for real-time score following -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Internet: WebSocket + REST to the local Python backend -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--
      FOREGROUND_SERVICE is required on Android 14+ if audio capture runs
      while the app is backgrounded (e.g. screen off during a long piece).
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>

    <application
        android:label="MusicPage"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <!-- Queries: needed to discover file manager apps for PDF import -->
    <queries>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT"/>
        </intent>
    </queries>
</manifest>
